# 系统启动说明

## ✅ 问题已修复

所有模型文件已修复，数据库已初始化完成！

## 🚀 如何启动系统

### 方法1：使用启动脚本（推荐）

**双击运行**：
```
start-server.bat
```

或在终端执行：
```bash
.\start-server.bat
```

### 方法2：手动启动

**步骤1：启动后端服务**

打开终端1，执行：
```bash
cd d:\tax-crm-system
npm start
```

等待看到：
```
✅ 数据库连接成功
🚀 服务器启动成功！
📍 服务地址: http://localhost:3000
```

**步骤2：启动前端服务**

打开终端2，执行：
```bash
cd d:\tax-crm-system\client
npm run dev
```

等待看到：
```
  ➜  Local:   http://localhost:5173/
```

**步骤3：访问系统**

在浏览器打开：http://localhost:5173

## 🔐 测试账户

| 邮箱 | 密码 | 角色 |
|------|------|------|
| admin@example.com | admin123 | 管理员 |
| lour@email.com | (您设置的密码) | 销售 |
| hhh@email.com | (您设置的密码) | 经理 |

## ⚠️ 常见问题

### 问题1：端口被占用

**错误信息**：`EADDRINUSE: address already in use 0.0.0.0:3000`

**解决方法**：
```powershell
# 停止所有node进程
Stop-Process -Name node -Force

# 然后重新启动
npm start
```

### 问题2：数据库连接失败

**错误信息**：`SequelizeConnectionError`

**解决方法**：
1. 确保 PostgreSQL 服务正在运行
2. 检查 `.env` 文件配置
3. 运行数据库检查：
```bash
node init-database.js
```

### 问题3：模型加载失败

**错误信息**：`sequelize.define is not a function`

**解决方法**：
已修复！如果还有问题，请检查模型文件格式。

### 问题4：前端无法连接后端

**错误信息**：`ERR_CONNECTION_REFUSED`

**解决方法**：
1. 确保后端服务已启动（端口3000）
2. 检查浏览器控制台错误
3. 访问 http://localhost:3000/health 测试

## 📋 启动检查清单

- [ ] PostgreSQL 服务正在运行
- [ ] 后端服务启动成功（端口3000）
- [ ] 前端服务启动成功（端口5173）
- [ ] 能访问 http://localhost:3000/health
- [ ] 能访问 http://localhost:5173
- [ ] 能成功登录系统

## 🛠️ 诊断工具

### 检查数据库状态
```bash
node init-database.js
```

### 检查用户数据
```bash
node check-users.js
```

### 测试后端健康
```bash
curl http://localhost:3000/health
```

或在浏览器访问：http://localhost:3000/health

## 📞 需要帮助？

如果遇到问题，请提供：
1. 终端的完整错误信息
2. 浏览器控制台的错误（F12 -> Console）
3. Network 标签页的请求状态

---

**系统状态**：✅ 已修复，可以正常启动

**最后更新**：2026-02-02










