# 数据库迁移问题解决方案

## 问题原因
SQL文件编码问题导致部分表创建失败。从错误信息看，`customer_files` 和 `follow_up_reminders` 表没有创建成功。

## 解决方案

### 方法1：使用 pgAdmin（推荐）

1. **打开 pgAdmin**
2. **连接到 tax_crm 数据库**
3. **打开查询工具（Query Tool）**
4. **复制以下SQL并执行**：

```sql
-- 创建客户文件表
CREATE TABLE IF NOT EXISTS customer_files (
  id SERIAL PRIMARY KEY,
  client_id INTEGER NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  follow_up_id INTEGER REFERENCES follow_ups(id) ON DELETE SET NULL,
  file_name VARCHAR(255) NOT NULL,
  file_path VARCHAR(500) NOT NULL,
  file_size INTEGER,
  file_type VARCHAR(50),
  category VARCHAR(20) DEFAULT 'other',
  description TEXT,
  uploaded_by INTEGER NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 创建跟进提醒表
CREATE TABLE IF NOT EXISTS follow_up_reminders (
  id SERIAL PRIMARY KEY,
  follow_up_id INTEGER NOT NULL REFERENCES follow_ups(id) ON DELETE CASCADE,
  user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  reminder_type VARCHAR(20) DEFAULT 'system',
  reminder_time TIMESTAMP WITH TIME ZONE NOT NULL,
  status VARCHAR(20) DEFAULT 'pending',
  sent_at TIMESTAMP WITH TIME ZONE,
  error_message TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_customer_files_client_id ON customer_files(client_id);
CREATE INDEX IF NOT EXISTS idx_customer_files_follow_up_id ON customer_files(follow_up_id);
CREATE INDEX IF NOT EXISTS idx_customer_files_uploaded_by ON customer_files(uploaded_by);

CREATE INDEX IF NOT EXISTS idx_follow_up_reminders_follow_up_id ON follow_up_reminders(follow_up_id);
CREATE INDEX IF NOT EXISTS idx_follow_up_reminders_user_id ON follow_up_reminders(user_id);
CREATE INDEX IF NOT EXISTS idx_follow_up_reminders_reminder_time ON follow_up_reminders(reminder_time);

-- 验证
SELECT 'Tables created successfully!' AS message;
```

### 方法2：使用命令行（设置编码）

```powershell
# 设置环境变量
$env:PGCLIENTENCODING = "UTF8"

# 执行迁移
psql -U postgres -d tax_crm -f migration-fix.sql
```

### 方法3：使用 DBeaver 或其他工具

1. 打开 DBeaver
2. 连接到 tax_crm 数据库
3. 新建 SQL 编辑器
4. 粘贴上面的 SQL
5. 执行

## 验证迁移是否成功

在 pgAdmin 或命令行中执行：

```sql
-- 检查所有表
SELECT table_name 
FROM information_schema.tables 
WHERE table_name IN (
  'follow_ups', 
  'follow_up_comments', 
  'customer_files', 
  'customer_discussions', 
  'follow_up_reminders'
)
ORDER BY table_name;
```

应该看到 5 个表：
- ✅ customer_discussions
- ✅ customer_files
- ✅ follow_up_comments
- ✅ follow_up_reminders
- ✅ follow_ups

## 完成后的步骤

### 1. 安装前端依赖

```powershell
cd client
npm install react-quill dayjs
```

### 2. 启动服务

```powershell
# 终端1：启动后端
npm start

# 终端2：启动前端
cd client
npm run dev
```

### 3. 测试功能

访问：http://localhost:5173/customers

点击任意客户，测试：
- ✅ 创建跟进记录
- ✅ 添加评论
- ✅ 上传文件
- ✅ 查看日历

## 常见问题

### Q: 表已经存在怎么办？
A: 使用 `CREATE TABLE IF NOT EXISTS`，如果表存在会自动跳过

### Q: 外键约束失败？
A: 确保 `clients`、`users`、`follow_ups` 表已经存在

### Q: 编码错误？
A: 使用 pgAdmin 或设置 `$env:PGCLIENTENCODING = "UTF8"`

## 快速检查命令

```sql
-- 检查 customer_files 表
SELECT COUNT(*) FROM customer_files;

-- 检查 follow_up_reminders 表
SELECT COUNT(*) FROM follow_up_reminders;

-- 如果上面的命令没有报错，说明表创建成功！
```

## 需要帮助？

如果仍然有问题，请：
1. 截图错误信息
2. 检查 PostgreSQL 版本
3. 确认数据库连接正常
4. 查看 pgAdmin 中的表列表

---

**建议：使用 pgAdmin 的查询工具是最简单可靠的方法！**














